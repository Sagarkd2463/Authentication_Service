<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Facebook Dashboard</title>
</head>

<style>
    .logout-btn {
        background-color: black;
        color: white;
        padding: 15px 25px 15px 25px;
        border-radius: 8px;
        margin-right: 20px;
        text-decoration: none;
        text-align: center;
        cursor: pointer;
        font-size: 17px;
    }
</style>

<body>
    <p>Successfully Logged In</p>
    <p>
        <strong>Name</strong>: <%= name %> <br>
            <strong>Email</strong>: <%= email %> <br>
                <strong>Provider</strong>: <%= provider %>
                    <strong>Pic</strong>:
                    <% if (pic) { %>
                        <img src="<%= pic %>" width="100" height="100" alt="<%= name %>'s picture" />
                        <% } else { %>
                            <p>No picture available</p>
                            <% } %>
    </p>
    <a class="logout-btn" href="/auth/google/logout">Logout</a>
</body>

</html>